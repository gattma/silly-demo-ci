apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo1
  name: demo1
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: demo1
  type: ClusterIP
---